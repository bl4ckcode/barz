name: barz
description: "Barz, your drink on your table instantly."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.8.0

# Dependencies - UPDATED TO LATEST SECURE VERSIONS FOR PAYMENTS APP
# Last updated: January 2026
dependencies:
  flutter:
    sdk: flutter

  # --- CORE & UTILITIES ---
  intl: ^0.20.2
  phone_form_field: ^10.0.17
  cupertino_icons: ^1.0.8
  collection: ^1.19.1
  logging: ^1.3.0
  
  # --- STATE MANAGEMENT & DI ---
  flutter_bloc: ^9.1.1
  get_it: ^9.0.0                    # Updated for latest DI features
  provider: ^6.1.2
  dartz: ^0.10.1
  equatable: ^2.0.7
  
  # --- NETWORKING ---
  dio: ^5.8.0                       # Latest with security patches
  http: ^1.3.0
  web_socket_channel: ^3.0.2
  web: ^1.1.1                       # Web platform interop (for JS API calls)
  
  # --- STORAGE (CRITICAL FOR PAYMENTS) ---
  shared_preferences: ^2.5.3
  flutter_secure_storage: ^9.2.4    # Encrypted storage for tokens
  hive: ^2.2.3                      # Local NoSQL database for offline cache
  hive_flutter: ^1.1.0              # Hive Flutter integration
  connectivity_plus: ^6.1.4         # Network connectivity detection
  workmanager: ^0.9.0               # Background task scheduling
  
  # --- JSON & SERIALIZATION ---
  json_annotation: ^4.9.0
  freezed_annotation: ^3.1.0        # Updated to v3

  # --- LOCALIZATION ---
  flutter_localizations:
    sdk: flutter
  
  # --- UI & DESIGN ---
  flutter_screenutil: ^5.9.3
  device_preview: ^1.3.1            # Updated
  webview_flutter: ^4.11.0          # Updated for security
  velocity_x: ^4.2.1
  flutter_svg: ^2.0.17              # Updated
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.2
  loading_animation_widget: ^1.3.0  # Added from openfisio
  flutter_web_plugins:              # For web URL strategy
    sdk: flutter
  flutter_form_builder: ^10.2.0     # Form building with validation
  form_builder_validators: ^11.1.0  # Validators for form builder
  mask_text_input_formatter: ^2.9.0 # Mask formatter for CNPJ/CPF/etc
  # rive removed - using Material Icons for nav instead

  # --- GOOGLE MAPS & LOCATION ---
  google_maps_flutter: ^2.12.0      # Updated
  # google_places_api_flutter removed - using our backend proxy instead
  location: ^8.0.0
  flutter_polyline_points: ^3.1.0   # Updated

  # --- FIREBASE (CRITICAL - UPDATED) ---
  firebase_core: ^4.3.0             # Latest stable
  firebase_auth: ^6.1.3             # Latest with security fixes

  # --- SOCIAL LOGIN ---
  google_sign_in: ^7.2.0            # Updated
  google_sign_in_web: ^1.1.0        # Explicit import for web-only renderButton()
  sign_in_with_apple: ^7.0.1

  # --- PAYMENTS SPECIFIC ---
  pinput: ^6.0.1                    # Updated for OTP/PIN
  sms_autofill: ^2.4.1
  permission_handler: ^12.0.1       # Updated
  
  # --- CAMERA & IMAGE ---
  image_picker: ^1.2.1              # Camera and gallery image selection
  image_cropper: ^8.0.2             # Crop images before upload
  
  # --- MISC ---
  pretty_animated_buttons: ^0.0.2
  image: ^4.5.3
  go_router: ^16.0.0                # Added for proper routing
  app_links: ^6.4.0                 # Added for deep links
  firebase_messaging: ^16.1.0
  flutter_local_notifications: ^19.5.0
  cached_network_image: ^3.4.1
  animated_text_kit: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4

  flutter_lints: ^6.0.0             # Updated
  json_serializable: ^6.11.3        # Updated
  build_runner: ^2.10.4             # Updated
  freezed: ^3.2.3                   # Updated to v3
  flutter_launcher_icons: ^0.14.4   # Added
  flutter_native_splash: ^2.4.6     # Added

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/login/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
  generate: true